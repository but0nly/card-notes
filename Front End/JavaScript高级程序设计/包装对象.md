#JavaScript 

每当我们使用原始值的方法或者属性的时候，后台都会创建一个相应的原始值包装对象

```js
// 例如
let str = "这是一个字符串"；
let str1 = str.substring(2);
```

我们知道，原始值本就不是对象，但是以上却正确的执行了，是因为后台执行了以下三步：

1. 创建对应原始值类型的实例
2. 调用实例上的原型方法
3. 销毁实例。

```js
let str = String("这是一个字符串")；
let str1 = str.substring(2);
str = null
```
#面试题 

父组件有更新，子组件无条件更新

- `shouldComponentUpdate`（SCU）
- `PureComponent` 和 `React.memo()`
- 不可变值 `immutable.js`
- 按需使用（异步组件）和 `state` 层级




1. 模拟`v-show`切换组件
2. 列表渲染加`key`
3. 自定义事件、*DOM*事件销毁时解绑
4. SCU 避免重新渲染
5. 减少bind(this)
6. 合理使用异步组件
7. 减少`state`层级
8. *Webpack*（使用外部工具）
9. 前端通用优化， 图片懒加载、列表懒加载
10. **SSR**
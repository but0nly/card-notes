#面试题 
- 都是组件化
- 都是数据驱动视图
- 都是 *vdom* 代替 *dom*
- **React** 使用 `JSX` 拥抱 *JS*，**Vue** 使用模板拥抱 *Html*
- **React** 函数式编程，**Vue** 是声明式编程
- **React** 更多需要自力更生（例如SCU），**Vue** 的话是把想要的都给你（`computed`、`watch` 等待）


如果项目开始需要选择技术栈那个好
- **考虑项目的复杂性**
- **人员的配备**
- **工期**
- **业务**
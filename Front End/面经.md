### 贷查查
**一面**
- React 动态路由
- ErrorBoundary 错误边界
- 图片分片上传，怎么做，怎么保存上传的进度
- IndexDB 频繁插入数据，会导致数据丢失，怎么处理这个频繁插入
- 全局换肤怎么做

### 
**一面**
- React Hooks 模拟生命周期
- `useRef` 和 `useState` 有什么区别
- 为什么 Hooks 要按照顺序，不能写判断
- `useMemo` 和 `useCallback` 的场景，还有什么情况下不适用
- `useLayoutEffect` 和 `useEffect` 的区别
- `React.memo` 如何做深对比呢
- `useContext` 怎么用，场景有哪一些
- 有些过哪些实用的 hook
- 有封装过哪些，二封过的组件

**二面**
- ==React Redux 是怎么存储的，如果要你去实现你会怎么做==
- ==Keep-alive 如果用 React 来的实现的话，你应该怎么做==
- 输入框不用React 18 的 useDeferValue，和不用节流的话，怎么实时获取
	- 网络延迟导致数据延后
	- 解决方案：中断接口
- 共用一个弹窗的话，useState 还会展示原来的数据
	- 原因是 useState 闭包，数据没有被清除
	- 解决方案：弹窗Promise化，用ahooks那个useSafeState;
- 图片分割，导致页面卡顿怎么解决
	- 原因：转buff流或者base64的时候，阻塞主线程
	- 解决方案：放到Web Work去运行
- 事件循环，有没有哪些Bug或者需求是使用到了事件循环
	- 说了一个函数内执行一个长任务，可以分割运行，利用setTimeout的宏任务机制
- 有一个函数，第一个参数是promise数组，第二个是promise的并发数量，然后会返回数组的结果，顺序要保持一致 


#面试题 
### 原理/定义
ES6的语法
允许从数组/对象中提取值，然后将这些值赋值给单独的变量，它使得从复杂的数据结构中提取数据变得更简洁和直观

- 用于声明变量
```js
const { name } = {
	name: 'foo'
}；

console.log(name); // foo
```
- 用于合并数组
```js

const arr = [1, 2, 3];

const newArr = [...arr, 4];

console.log(newArr); 1,2,3,4

```
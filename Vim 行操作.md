- 行移动
	- 移动到行首 使用 `0`
	- 移动到行尾 使用 `$`
	- 移动到行首 使用键 `H`， 第一位不是blank字符[^1]  
	- 移动到行尾 使用键 `L` ，最后一位不是blank字符 
- 行插入
	- 插入到行首 使用 `I`
	- 插入到行尾 使用 `A`
	- 插入到行后 使用 `o`
	- 插入到行前 使用 `O`
- 复制行 使用 `yy`
- 粘贴行 使用 `p`
- 删除行 使用 `dd`
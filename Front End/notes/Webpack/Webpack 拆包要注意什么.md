#面试题 

- **避免重复**：确保每个包的代码都是唯一的，不会重复
- **合理的包体积**：不能太大也不能太小，太小可能会导致过多发出Http请求
- **抽离公共代代码**：例如一些第三方库，打成单独的一个包，即使其它包更新时，这个包也不需要再重新加载。
- **按需加载**：只有在需要的时候才会加载，通过动态导入import()实现
- **考虑长期缓存**：通过合理的文件命名配置，使得一些不经常变化的代码被浏览器缓存（协商缓存），再次访问的之间从浏览器缓存取，从而提高性能。
- **分析工具**：通过 *analyzer* 分析包大小和组成。方便进行优化。